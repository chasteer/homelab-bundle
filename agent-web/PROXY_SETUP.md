# ⚠️ Устарело

Прокси для **Groq** в чат-агенте больше не используется (Groq/LangGraph удалены).

Homelab Incident Service обращается к **Cursor API** из контейнера напрямую (нужен исходящий HTTPS).

Настройка инцидентов: [INCIDENT_FLOW.md](INCIDENT_FLOW.md).
