## ✅ Проверено:
- ✅ Вебхук агента работает: `http://192.168.1.200:8000/api/webhook/uptime-kuma`
- ✅ Агент обрабатывает уведомления
- ✅ Логирование работает

## 📋 Шаг 1: Откройте Uptime Kuma

1. Откройте браузер и перейдите по адресу:
   ```
   http://192.168.1.200:3001
   ```

2. Войдите в систему или создайте аккаунт

## 📋 Шаг 2: Создание уведомления

1. В левом меню нажмите **Settings** (⚙️)
2. Выберите **Notifications**
3. Нажмите **"Add New Notification"**

## 📋 Шаг 3: Настройка вебхука

### Основные параметры:
- **Type:** `Webhook`
- **Name:** `Homelab Agent Webhook`
- **URL:** `http://172.20.0.4:8000/api/webhook/uptime-kuma` ⭐
- **Method:** `POST`
- **Content Type:** `application/json`

### Дополнительные настройки:
- **Retry:** `3`
- **Retry Interval:** `60` секунд
- **Events:** выберите все:
  - ✅ `Up` - сервис восстановился
  - ✅ `Down` - сервис упал
  - ✅ `Paused` - сервис приостановлен
  - ✅ `Maintenance` - сервис в обслуживании

## 📋 Шаг 4: Тестирование

1. Нажмите **"Test"** для проверки
2. Должно появиться сообщение об успешной отправке
3. Нажмите **"Save"** для сохранения

## 🔍 Шаг 5: Проверка работы

### Тест 1: Остановка сервиса
```bash
# Остановите TorrServer
sudo docker compose -f services/docker-compose.yml stop torrserver

# Подождите 30-60 секунд
sleep 60

# Проверьте логи агента
sudo docker logs homelab-agent | grep -i "webhook\|uptime" | tail -5
```

### Тест 2: Запуск сервиса
```bash
# Запустите TorrServer обратно
sudo docker compose -f services/docker-compose.yml start torrserver

# Подождите 30-60 секунд
sleep 60

# Проверьте логи агента снова
sudo docker logs homelab-agent | grep -i "webhook\|uptime" | tail -5
```

## 📊 Ожидаемые результаты

### При остановке сервиса:
```
🚨 Получено уведомление от Uptime Kuma: TorrServer - down
```

### При запуске сервиса:
```
✅ Получено уведомление от Uptime Kuma: TorrServer - up
```

## 🚨 Устранение неполадок

### Проблема: Вебхук не работает
**Решение:**
1. Проверьте URL: `http://172.20.0.4:8000/api/webhook/uptime-kuma`
2. Убедитесь, что агент запущен: `sudo docker compose ps`
3. Проверьте вебхук: `curl http://192.168.1.200:8000/api/webhook/uptime-kuma/health`

### Проблема: Уведомления не приходят
**Решение:**
1. Проверьте настройки Events в Uptime Kuma
2. Убедитесь, что мониторинг сервисов включен
3. Проверьте логи Uptime Kuma

### Проблема: Агент не обрабатывает уведомления
**Решение:**
1. Проверьте логи агента: `sudo docker logs homelab-agent`
2. Убедитесь, что вебхук настроен правильно
3. Проверьте формат данных в логах

## 📚 Дополнительные ресурсы

- **Документация:** `UPTIME_KUMA_INTEGRATION.md`
- **Правильная конфигурация:** `WEBHOOK_CORRECT_CONFIG.md`
- **Автоматическая настройка:** `setup_uptime_webhook_auto.py`

## 🎯 Результат

После настройки вы получите:
- ✅ Автоматические уведомления о состоянии сервисов
- ✅ Логирование всех событий в агенте
- ✅ Возможность настройки дополнительных действий
- ✅ Интеграцию с VPS для внешних уведомлений

---

**Готово! Теперь настройте вебхук в Uptime Kuma и протестируйте его работу.** 🚀
